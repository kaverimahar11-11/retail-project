pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building Retail Project'
            }
        }

        stage('Test') {
            steps {
                echo 'Running Unit Tests'
            }
        }

        stage('Deploy') {
            steps {
                echo 'Deploying to Production'
            }
        }
    }
}
